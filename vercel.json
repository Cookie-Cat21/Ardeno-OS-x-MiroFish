{
  "version": 2,
  "buildCommand": "cd apps/os && npm install && npm run build",
  "outputDirectory": "apps/os/dist",
  "framework": null,
  "rewrites": [
    { "source": "/api/:path*", "destination": "/api/index" }
  ]
}
