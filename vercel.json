{
  "version": 2,
  "installCommand": "cd apps/os && npm install --legacy-peer-deps",
  "buildCommand": "cd apps/os && npm run build",
  "outputDirectory": "apps/os/dist",
  "framework": null,
  "rewrites": [
    { "source": "/api/:path*", "destination": "/api/index" }
  ]
}
